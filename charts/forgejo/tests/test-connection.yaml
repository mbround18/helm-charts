apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "forgej.fullname" . }}-test-connection"
  labels:
    {{- include "forgej.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "forgej.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
