apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bubble-system.fullname" . }}-test-connection"
  labels:
    {{- include "bubble-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bubble-system.fullname" . }}:{{ .Values.service.port }}/api/health']
  restartPolicy: Never
