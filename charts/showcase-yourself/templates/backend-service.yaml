apiVersion: v1
kind: Service
metadata:
  name: {{ include "showcase-yourself.fullname" . }}-backend
  labels:
    {{- include "showcase-yourself.componentLabels" (dict "context" . "component" "backend") | nindent 4 }}
  {{- $annotations := include "gitops-tools.argocd.annotations" (dict "context" . "phase" "ingress") }}
  {{- if $annotations }}
  annotations:
    {{- $annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "showcase-yourself.componentSelectorLabels" (dict "context" . "component" "backend") | nindent 4 }}
