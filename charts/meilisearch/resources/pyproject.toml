[project]
name = "meilisearch-provisioner"
version = "0.1.0"
description = "Meilisearch API Key Provisioner"
requires-python = ">=3.14"
dependencies = ["meilisearch", "rich", "kubernetes"]

[project.optional-dependencies]
dev = ["pytest", "pytest-mock", "pytest-cov", "mypy"]
