.PHONY: include

# Chart-specific overrides (set before including central Makefile)
CHART_DIR := $(CURDIR)
RELEASE_NAME ?= hytale
BUILD_DIR ?= tmp

# include centralized targets from repo root
include $(abspath $(CURDIR)/../..)/config/Makefile
